import { clear } from './clear'
import { insert } from './insert'
import { reset } from './reset'
import { run } from './run'
import { sync } from './sync'

/**
 * Actions used by the CLI.
 */
export default { clear, insert, reset, sync }

export { clear, insert, reset, run, sync }
